# Core extraction / data handling
openpyxl==3.1.5
pandas==2.2.3
pdfplumber==0.11.9

# Optional alternative PDF backend (uncomment if you prefer PyMuPDF over
# pdfplumber -- see pdf_extractor.py's _read_pages() for the swap point).
# PyMuPDF==1.24.10

# Optional: only needed if you wire up call_llm_for_ambiguous_matches()
# in normalize.py to a real LLM provider.
# anthropic==0.34.0
